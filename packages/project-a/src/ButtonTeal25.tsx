import cn from "classnames";

export const ButtonTeal25 = () => {
  return (
    <button
      type="button"
      className={cn(
        "text-black",
        "bg-al-teal-25",
      )}
    >
      Button Teal 25
    </button>
  );
};
